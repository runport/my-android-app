# گزارش وضعیت فنی و معماری پروژه (PROJECT_STATUS.md)

**تاریخ گزارش:** اسفند ۱۴۰۳  
**نسخه پایگاه داده:** Version 5 (Room Database - با مایگریشن ایمن MIGRATION_4_5 بدون حذف داده)  
**فناوری‌های اصلی:** Android Jetpack Compose, Kotlin Coroutines & Flow, Room ORM, Material 3  

---

## ۱. نمای کلی و معماری فعلی سامانه

این اپلیکیشن بر اساس معماری استاندارد **MVVM (Model-View-ViewModel)** و اصل تفکیک وظایف (**Separation of Concerns**) استوار است:

```
┌─────────────────────────────────────────────────────────────┐
│                       UI Layer                              │
│  - Jetpack Compose (Material 3)                             │
│  - DashboardScreen, InventoryScreen, AnalyticsScreen        │
│  - QuickActionSheets (Hub, Roll In, Roll Consume, Shipping) │
│  - ExecutiveCombinedChart, Dialogs                          │
└──────────────────────────────┬──────────────────────────────┘
                               │ StateFlow / Events
┌──────────────────────────────▼──────────────────────────────┐
│                    ViewModel Layer                          │
│  - ManufacturingViewModel                                   │
│  - AppTimeRangeState, ChartState, KpiState                  │
│  - fabricRolls, rollUsages, shippingExpenses, shippingAvg   │
└──────────────┬──────────────────────────────┬───────────────┘
               │                              │
┌──────────────▼──────────────┐┌──────────────▼───────────────┐
│       Domain & Service      ││       Data & Storage         │
│  - FinancialCalculation     ││  - ManufacturingRepository   │
│    Service                  ││  - AppDatabase (Room v5)     │
│  - PersianDateHelper        ││  - DAOs (Sales, Production,  │
│  - TimeRange models         ││    Inventory, Rolls, Shipping)
└─────────────────────────────┘└──────────────────────────────┘
```

- **عدم وابستگی محاسبات به UI:** کلیه فرمول‌های سود ناخالص، سود خالص، حاشیه سود، بهای تمام‌شده، تبدیل واحدها (متر به کیلوگرم و بالعکس)، تسهیم هزینه باربری و تولید نقاط چارت در `FinancialCalculationService` متمرکز است.
- **واکنش‌پذیری (Reactive Pipeline):** تغییر در فیلتر زمانی (`timeRangeState`) یا ثبت طاقه جدید، مصرف طاقه و ثبت هزینه باربری به‌صورت بلادرنگ با `Flow.combine` و `StateFlow` در تمام بخش‌های برنامه محاسبه و منعکس می‌شود.
- **حفظ تمامیت داده‌ها:** تغییرات ساختار جدول‌ها در نسخه ۵ دیتابیس با مایگریشن ایمن `MIGRATION_4_5` انجام شده و کلیه جداول قبلی بدون دستکاری حفظ گردیده‌اند.

---

## ۲. تناقض‌های شناسایی و برطرف‌شده در فاز ۱ و ۲

| موضوع | وضعیت قبلی (متناقض) | وضعیت فعلی (اصلاح‌شده و یکپارچه) |
|---|---|---|
| **فیلتر زمانی داشبورد** | مقایسه غیراستاندارد رشته‌های متنی تاریخ که در فیلترها خطا ایجاد می‌کرد. | تبدیل تاریخ به تایم‌استمپ میلادی/جلالی بر حسب میلی‌ثانیه (`fromTimestamp` و `toTimestamp`) و فیلترینگ دقیق ریاضی. |
| **هاردکد عنوان چارت و کارت‌ها** | عناوینی نظیر «مجموع فروش امروز» ثابت بودند. | عناوین به‌صورت کاملاً دینامیک از روی بازه فعال تولید می‌شوند. |
| **عدم وجود چارت ترکیبی** | چارت تک‌بعدی با مقادیر تصادفی. | کامپوننت `ExecutiveCombinedChart` که فروش و سود خالص را هم‌زمان بر روی یک محور مقایسه می‌کند. |
| **مدیریت طاقه‌های پارچه** | طاقه‌ها فقط در قالب یک متراژ کلی در جدول `FabricEntity` ثبت می‌شدند و امکان ردیابی هر طاقه، متراژ باقی‌مانده و پارت نامبر وجود نداشت. | ایجاد موجودیت و جدول مستقل `FabricRollEntity` با ردیابی شناسه طاقه، متراژ اولیه، متراژ باقی‌مانده، وزن، پارت نامبر، تأمین‌کننده، قیمت خرید متری و کیلویی و سهم باربری. |
| **ثبت برش و مصرف طاقه** | برش‌ها به طاقه مشخص متصل نبودند و کاهش متراژ از طاقه انجام نمی‌شد. | ایجاد `RollUsageEntity` و فرم `QuickRollConsumeForm` که با ثبت مصرف، متراژ باقی‌مانده طاقه را به صورت اتمیک کسر کرده و وضعیت طاقه را به "در حال مصرف" یا "پایان یافته" به‌روزرسانی می‌کند. |
| **هزینه باربری و سرشکن کردن** | هزینه‌های باربری به متریال یا طاقه‌ها تخصیص نمی‌یافت و در بهای تمام‌شده لحاظ نمی‌شد. | ایجاد `ShippingExpenseEntity`، روش‌های تخصیص بر اساس تعداد طاقه، وزن و تعداد قطعه، تخصیص مستقیم باربری به طاقه‌ها، و گزارش‌گیری تحلیلی میانگین هر طاقه/کیلو در `AnalyticsScreen`. |
| **واحدهای مختلف متریال** | تنها واحد متر برای پارچه در نظر گرفته شده بود. | پشتیبانی کامل از واحدهای متر، کیلوگرم، طاقه، عدد، یارد و گرم به همراه تبدیل دقیق متر به کیلو و برعکس با توجه به گرماژ و مشخصات طاقه. |

---

## ۳. فایل‌های مهم و کلیدی پروژه

### لایه مدل و داده (Data & Models)
- `/app/src/main/java/com/example/data/model/Entities.kt`:
  موجودیت‌های جدید `FabricRollEntity` (طاقه‌ها)، `RollUsageEntity` (سوابق مصرف طاقه)، `AccessoryPurchaseEntity` (خرید ملزومات) و `ShippingExpenseEntity` (بارنامه‌ها و هزینه‌های حمل).
- `/app/src/main/java/com/example/data/local/AppDatabase.kt`:
  ارتقای نسخه دیتابیس به نسخه ۵ و پیاده‌سازی `MIGRATION_4_5` ایمن برای اضافه کردن جداول فاز ۲.
- `/app/src/main/java/com/example/data/local/Daos.kt`:
  افزوده شدن `FabricRollDao`، `RollUsageDao`، `AccessoryPurchaseDao` و `ShippingExpenseDao` با کوئری‌های واکنشی Flow.

### لایه سرویس و منطق محاسباتی (Domain & Services)
- `/app/src/main/java/com/example/data/service/FinancialCalculationService.kt`:
  - توابع تبدیل متراژ و وزن: `calculatePricePerMeter` و `calculatePricePerKg`.
  - توابع تخصیص باربری: `calculateUnitShippingCost` بر اساس روش‌های `PER_ITEM`، `PER_WEIGHT`، `PER_QUANTITY`، `WEIGHTED`.
  - محاسبه میانگین‌های باربری دوره‌ای: `calculateShippingAverages` جهت استخراج میانگین هزینه هر طاقه، کیلوگرم و واحد.
  - آماده‌سازی فرمول‌های بهای تمام‌شده موثر (قیمت پایه پارچه + باربری سرشکن شده).

### لایه مخزن و ویومدل (Repository & ViewModel)
- `/app/src/main/java/com/example/data/repository/ManufacturingRepository.kt`:
  متدهای ثبت، به‌روزرسانی، مصرف طاقه و ثبت بارنامه با مدیریت تراکنش و به‌روزرسانی وضعیت طاقه‌ها.
- `/app/src/main/java/com/example/viewmodel/ManufacturingViewModel.kt`:
  افزوده شدن جریان‌های `fabricRolls`، `availableFabricRolls`، `rollUsages`، `shippingExpenses`، `shippingAverages`، اکشن‌های سریع جدید و هندلرهای ذخیره‌سازی.

### لایه رابط کاربری (UI & Screens)
- `/app/src/main/java/com/example/ui/screens/InventoryScreen.kt`:
  - افزوده شدن تب‌های مستقل «طاقه‌ها (فاز ۲)» و «باربری» به منوی انبار.
  - کارت‌های اختصاصی `FabricRollInventoryCard` با نوار پیشرفت متراژ باقی‌مانده، برچسب وضعیت، قیمت خرید، سهم باربری و دکمه‌های سریع مصرف و تاریخچه برش.
  - کارت‌های `ShippingExpenseInventoryCard` با خلاصه شاخص‌های باربری و روش‌های تخصیص.
- `/app/src/main/java/com/example/ui/screens/AnalyticsScreen.kt`:
  - افزوده شدن بخش «گزارش باربری» با فیلتر بازه زمانی (امروز ۲۴س، ۷ روز، ۳۰ روز، کل دوره).
  - ۴ کارت شاخص کلیدی: مجموع هزینه‌های باربری، میانگین هزینه هر طاقه، میانگین هزینه هر کیلو، و میانگین هزینه هر واحد.
  - لیست تفکیکی بارنامه‌ها همراه با شرکت باربری و روش تسهیم.
- `/app/src/main/java/com/example/ui/dialogs/QuickActionSheets.kt`:
  - کاشی‌های جدید در هاب عملیات انبار (`QuickWarehouseHub`).
  - فرم ورود طاقه جدید (`QuickRollForm`) با محاسبه خودکار نرخ متری/کیلویی و تخصیص اولیه باربری.
  - فرم مصرف و برش طاقه (`QuickRollConsumeForm`) با انتخاب طاقه موجود، مدل، متراژ برش و متراژ ضایعات.
  - مودال تاریخچه و سوابق مصرف طاقه (`RollHistoryModal`).
  - فرم ثبت هزینه باربری و بارنامه (`QuickShippingExpenseForm`) با تعیین روش تسهیم.

---

## ۴. وضعیت قابلیت‌های درخواستی فاز ۲

1. **مدیریت طاقه:**
   - [x] شناسه طاقه (`rollCode`)
   - [x] تاریخ ورود (`inboundDate` / شمسی)
   - [x] نوع پارچه (`fabricType`) و کد پارچه
   - [x] مقدار اولیه (`initialMeters`)
   - [x] مقدار باقی‌مانده (`remainingMeters`) با محاسبه لحظه‌ای
   - [x] وزن طاقه (`weightKg`) و وزن باقیمانده
   - [x] قیمت خرید متری و کیلویی (`buyPricePerMeter`, `buyPricePerKg`)
   - [x] هزینه باربری تخصیص‌یافته (`allocatedShippingCost`)
   - [x] نام تأمین‌کننده و پارت نامبر (`batchNumber`)
   - [x] وضعیت طاقه (موجود، در حال مصرف، پایان یافته)
   - [x] تاریخچه مصرف طاقه با متراژ مصرفی، مدل تولیدی و تاریخ برش

2. **ورود مواد به انبار:**
   - [x] ثبت ورود طاقه پارچه با اطلاعات کامل
   - [x] امکان ورود طاقه بر اساس متر یا کیلوگرم با تبدیل خودکار
   - [x] به‌روزرسانی آنی موجودی انبار

3. **مدیریت ملزومات (خرج‌کار):**
   - [x] ثبت خرید ملزومات با شناسه، عنوان، دسته‌بندی و قیمت واحد
   - [x] اتصال خریدهای جدید به انبار ملزومات
   - [x] نمایش در تب ملزومات انبار

4. **پشتیبانی از Unitهای مختلف:**
   - [x] متر (Meter)
   - [x] کیلوگرم (Kilogram)
   - [x] عدد (Unit / Piece)
   - [x] طاقه (Roll)
   - [x] یارد (Yard)
   - [x] گرم (Gram)

5. **قیمت خرید قدیم / جدید:**
   - [x] نگهداری قیمت خرید قبلی و تاریخ آن در مدل داده
   - [x] هشدار بصری در صورت تغییر قیمت خرید متریال در کارت‌های انبار
   - [x] محاسبه بهای موثر با قیمت جدید

6. **هزینه باربری:**
   - [x] ثبت شماره بارنامه و کد رهگیری
   - [x] شرکت باربری / راننده
   - [x] مبلغ کل بارنامه و تاریخ ثبت
   - [x] تعیین محموله (طاقه، ملزومات، ترکیبی)

7. **تخصیص باربری (Shipping Allocation):**
   - [x] تخصیص بر اساس تعداد طاقه (`PER_ITEM` / `PER_ROLL`)
   - [x] تخصیص بر اساس وزن به کیلوگرم (`PER_WEIGHT`)
   - [x] تخصیص بر اساس تعداد قطعه یا متراژ (`PER_QUANTITY`)
   - [x] تخصیص مستقیم به یک طاقه خاص
   - [x] افزایش بهای تمام‌شده طاقه با سهم باربری

8. **آماده‌سازی محاسبه بهای تمام‌شده:**
   - [x] محاسبه قیمت تمام‌شده موثر هر متر پارچه شامل کرایه حمل
   - [x] محاسبه قیمت خرید هر کیلوگرم بر اساس گرماژ
   - [x] آماده‌سازی متغیرهای سربار و کرایه جهت کسر در فرمول‌های تولید

9. **گزارش هزینه‌های باربری:**
   - [x] فیلتر بر اساس بازه زمانی (امروز، ۷ روز اخیر، ۳۰ روز اخیر، کل دوره)
   - [x] مجموع هزینه‌های باربری
   - [x] میانگین هزینه هر طاقه
   - [x] میانگین هزینه هر کیلوگرم
   - [x] میانگین هزینه هر واحد

---

## ۵. وضعیت قابلیت‌های پیاده‌سازی‌شده در فاز ۳ (یکپارچه‌سازی جامع تولید و مالی)

1. **ثبت کار آماده (`QuickReadyGoodsForm`):**
   - [x] ثبت خودکار تاریخ و ساعت سیستم به تاریخ شمسی.
   - [x] فیلد «از طاقه» با فیلتر هوشمند فقط طاقه‌های فعال و دارای موجودی.
   - [x] قابلیت افزودن (+) و حذف (-) چند مدل محصول از یک طاقه پارچه.
   - [x] کنترل سفت‌وسخت موجودی طاقه (عدم امکان ثبت متراژ فراتر از مانده طاقه، نمایش ارور و غیرفعال شدن ثبت).

2. **مصرف ملزومات و هزینه خیاط‌کار (`QuickProductionForm`):**
   - [x] پشتیبانی از افزودن ملزومات چندگانه (زیپ، مارک، دکمه، کش و ...) با امکان انتخاب مستقیم از سابقه خریدهای انبار.
   - [x] استفاده خودکار از قیمت خرید واقعی ملزومات در بهای تمام‌شده.
   - [x] تغییر واژه «سربار» به «هزینه خیاط‌کار» در کل منطق سیستم و واسط کاربری.

3. **وضعیت‌های تولید و گردش کار سفارشات (`SaleOrderStatus`):**
   - [x] ۳ وضعیت اصلی و استاندارد: «سفارش داده شده»، «در حال دوخت»، «آماده ارسال».
   - [x] کارت پیشرفت فرآیند در کارت سفارشات (`OrderTimelineCard`) با تغییر وضعیت با یک کلیک.
   - [x] ثبت و نمایش سابقه کامل تغییر وضعیت (`OrderStatusHistoryEntity`) شامل: وضعیت قبلی، وضعیت جدید، تاریخ شمسی، ساعت و توضیحات.

4. **هزینه‌های تخصیص‌یافته ثابت (`QuickFixedCostForm`):**
   - [x] ثبت هزینه‌های ثابت کارگاه (اجاره کارگاه، استهلاک چرخ خیاطی، طراحی الگو و شابلون، حقوق سرپرست).
   - [x] ۴ دامنه تخصیص هوشمند: همه محصولات (سرشکن متناسب)، محصولات خاص (کدهای انتخابی)، دسته محصول خاص، و یک تولید مشخص (شناسه بچ).

5. **فرمولاسیون بهای تمام‌شده و سود (`FinancialCalculationService`):**
   - [x] فرمول دقیق بهای تمام‌شده:
     `بهای تمام‌شده = پارچه + باربری + ملزومات + هزینه خیاط‌کار + هزینه‌های ثابت تخصیص‌یافته`
   - [x] تفکیک هزینه هر واحد و هزینه کل.
   - [x] محاسبه لحظه‌ای سود هر واحد، سود کل و درصد حاشیه سود (`profitMargin`).

6. **قیمت‌گذاری بر اساس کیلوگرم / متر:**
   - [x] انتخاب دوگانه قیمت بر اساس متر / قیمت بر اساس کیلوگرم در فرم‌های خرید، فروش و انبار.
   - [x] محاسبه خودکار قیمت هر متر بر اساس نرخ کیلوگرم و متراژ در هر کیلوگرم:
     `قیمت هر متر = قیمت هر کیلوگرم ÷ متراژ در هر کیلوگرم`

7. **تست و اعتبارسنجی (سناریوی طاقه ۱۰۰ متری):**
   - [x] پیاده‌سازی تست خودکار در `ExampleUnitTest.kt`:
     - طاقه ۱۰۰ متری با تخصیص ۶۰ متر و ۴۰ متر -> تأیید موفق (`isValid = true`).
     - طاقه ۱۰۰ متری با تخصیص ۶۰ متر و ۴۱ متر -> رد و صدور پیام خطا با ذکر متراژ کسری.
     - محاسبه بهای تمام‌شده و سود و مارجین ۳۴.۲۸٪ در سناریوی دو محصول.
     - محاسبه تبدیل نرخ کیلو به متر (۴۵۰,۰۰۰ تومان/کیلو با ۳ متر/کیلو = ۱۵۰,۰۰۰ تومان/متر).
   - [x] اجرای موفقیت‌آمیز `gradle :app:testDebugUnitTest` و `compile_applet`.

---

## ۶. فایل‌های کلیدی توسعه‌یافته در فاز ۳
- `/app/src/main/java/com/example/data/model/Entities.kt`: موجودیت‌های `FixedCostEntity`، `FixedCostScope`، `OrderStatusHistoryEntity`، `SaleOrderStatus` و مدل‌های تخصیص چندگانه.
- `/app/src/main/java/com/example/data/service/FinancialCalculationService.kt`: موتور متمرکز محاسبات بهای تمام‌شده، اعتبارسنجی ظرفیت طاقه و تسهیم هزینه‌های ثابت.
- `/app/src/main/java/com/example/data/repository/ManufacturingRepository.kt`: متدهای تراکنشی ثبت چند محصولی از یک طاقه، ثبت مصرف ملزومات، تاریخچه وضعیت سفارشات و تخصیص هزینه‌های ثابت.
- `/app/src/main/java/com/example/viewmodel/ManufacturingViewModel.kt`: متدهای `submitMultiProductReadyGoods`، `submitProductionWithConsumables`، `submitFixedCost`، `updateOrderStatus` و StateFlowهای مرتبط.
- `/app/src/main/java/com/example/ui/dialogs/QuickActionSheets.kt`: فرم‌های `QuickReadyGoodsForm`، `QuickProductionForm`، `QuickFixedCostForm` و به‌روزرسانی کاشی‌ها و قیمت‌گذاری کیلو/متر در `QuickSaleForm`.
- `/app/src/main/java/com/example/ui/screens/MoreHubScreen.kt`: به‌روزرسانی `OrderTimelineCard` با ۳ وضعیت اصلی و آکاردئون سابقه تغییر وضعیت (تاریخ/ساعت).
- `/app/src/test/java/com/example/ExampleUnitTest.kt`: تست‌های واحد برای سناریوی طاقه ۱۰۰ متری و تجمیع بهای تمام‌شده.


